/**
 * Stub for Phaser's optional `phaser3spectorjs` debug module.
 * Aliased in vite.config.ts so vitest can import Phaser's source build.
 * Never used at runtime (WebGL inspector only).
 */
export default {};
